# SimdJson

An Elixir library for working with JSON using SIMD-accelerated parsing.

## Development

Install dependencies and run the test suite:

```sh
mix deps.get
mix test
```

Generate documentation with ExDoc once it is added as a dependency:

```sh
mix docs
```
